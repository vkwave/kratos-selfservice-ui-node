<footer class="vkwave-footer">
  <picture>
    <source srcset="{{brand.darkLogo}}" media="(prefers-color-scheme: dark)" />
    <img src="{{brand.lightLogo}}" alt="VKWAVE" width="160" height="32" />
  </picture>
  <nav aria-label="Legal">
    <a href="{{brand.privacyUrl}}">{{copy.privacyLabel}}</a>
    <a href="{{brand.termsUrl}}">{{copy.termsLabel}}</a>
    <a href="{{brand.supportUrl}}">{{copy.supportLabel}}</a>
  </nav>
</footer>
