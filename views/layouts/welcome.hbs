<!DOCTYPE html>
<html lang="{{lang}}">
  <head>
    {{> standard_headers}}
    <link type="text/css" rel="stylesheet" href="content-layout.css" />
    <link rel="stylesheet" href="/vkwave-theme.css" />
  </head>
  <body>
    <main data-testid="app-express">
      <div class="app-container">
        {{{nav}}}
        <div class="container spacing-32">
          <div class="content">
            {{{body}}}
          </div>
          {{> vkwave_branding}}
        </div>
      </div>
      {{> scripts }}
    </main>
  </body>
</html>
